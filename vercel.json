{
	"buildCommand": "bun run build",
	"outputDirectory": "dist",
	"installCommand": "bun install",
	"framework": null,
	"rewrites": [
		{
			"source": "/api/:path*",
			"destination": "/api/chat.ts"
		}
	],
	"functions": {
		"api/chat.ts": {
			"runtime": "bun",
			"maxDuration": 30
		}
	}
}
